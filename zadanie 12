print("Oblicz n'ty element ciągu Fibonacciego")
def fib(a):
   if a < 1:
      return 0
   if a < 2:
      return 1 
   wynik = fib(a - 1) + fib(a - 2)
   return wynik 
    
    
 def fib(a):
    b = 0
    c = 1
  
    for i in range(a):
        b, c = c, b+c
        
    return b
